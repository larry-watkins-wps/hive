"""Component tests — exercise region_template modules against real brokers/processes.

These tests are marked ``@pytest.mark.component`` and require Docker to be
reachable (they spin up ephemeral containers via ``testcontainers``).
"""
