# Observatory — Implementation Decisions Log

Append-only log of non-obvious calls made during implementation. Format: date · question · call made · rationale.

| Date | Question | Call | Rationale |
|---|---|---|---|
| 2026-04-20 | Should v1 render visible edge lines between regions? | No; v1 uses sparks as the primary edge-activity signal and adjacency drives spring distance only. | Spec §4.3 mentions edge thickness, but sparks carry directional + topic-color information that lines alone cannot. Revisit after visual QA in Task 16; if the scene feels empty between sparks, add thin faded edges as a v1.1 follow-up. |
| 2026-04-20 | Should v1 use the full sandboxed `region_reader.py` from spec §6.5? | No; v1 uses narrow single-purpose reads (`glia/regions_registry.yaml` at startup, `regions/*/subscriptions.yaml` at startup). Full sandboxed reader arrives as v2 Task 1. | V1 REST endpoints (`/api/health`, `/api/regions`) never touch the filesystem per request. Shipping the reader in v1 adds attack surface for no behavioral benefit. |
| 2026-04-20 | Task 1 plan code blocks failed ruff (UP037 quoted `"Settings"`, UP035 `typing.Iterable`, PLR2004 magic `4`). Follow plan verbatim or fix? | Applied minimal mechanical fixes: dropped quotes (safe under `from __future__ import annotations`), imported `Iterable` from `collections.abc`, added `# noqa: PLR2004` on the `len(buf) == 4` assertion. | Plan Step 12 requires ruff clean; workspace root owns lint config and cannot be relaxed per top-level gotcha. Semantics identical. Flag for plan author to update Task 1 code blocks so Tasks 2+ don't inherit the same style drift. |
| 2026-04-20 | Task 2 `RegionRegistry.seed_from` parses a `regions:` **list** of `{name, role, llm_model}` dicts, but the real `glia/regions_registry.yaml` uses a `regions:` **dict** keyed by name with `layer`/`required_capabilities`/`default_capabilities` fields. Mismatch will result in empty registry against production YAML. | Implemented the plan's list-format parser verbatim; `test_seed_loads_names_and_roles` uses a list-format fixture so it still passes. Logged here so Task 3+ (or a later schema reconciliation task) can address it. | Plan code is authoritative for Task 2 scope, and the plan's test fixture is explicitly list-format. Changing the real-YAML schema or the parser is out of scope and would touch `glia/` (forbidden for observatory sub-project). Needs a follow-up task to decide whether observatory adapts to the dict format, or whether `regions_registry.yaml` is duplicated/migrated. |
| 2026-04-20 | Task 2 plan code block `RegionRegistry.seed_from(...) -> "RegionRegistry"` triggers ruff UP037 under `from __future__ import annotations`. | Dropped the quotes (unquoted forward ref). Semantics unchanged because PEP 563 string-ifies all annotations. | Same mechanical fix as Task 1 precedent. |
| 2026-04-20 | Task 2 review-fix: adapt `RegionRegistry` to the real `glia/regions_registry.yaml` schema instead of the plan's fictional list format. | `seed_from` now reads `regions:` as a dict keyed by region name and maps `layer` → `RegionMeta.role`. Unknown/malformed entries silently skipped; non-mapping top-level or non-mapping `regions:` returns empty. Test fixture rewritten to match the real schema (sample of `thalamus` + `amygdala` entries). | Spec §6.5 authorises reading `glia/regions_registry.yaml`; the plan's fictional list schema would silently return empty against production data, breaking Tasks 7/8 end-to-end. Spec wins over plan prose per `observatory/CLAUDE.md` authority ordering. `llm_model` field remains empty-string default since the real YAML does not carry model per-region — future task can resolve via capabilities/model-routing policy if needed. |
| 2026-04-20 | Task 2 review-fix: `apply_heartbeat` unconditional `int(...)` coercion crashed on `None` or non-numeric strings. | Introduced `_coerce_int(value, fallback)` helper that catches `TypeError`/`ValueError` and falls back to the prior value. Observatory is a read-only sidecar and must degrade on Hive-side schema drift. Added regression test `test_malformed_heartbeat_payload_preserves_prior_values`. | Code-quality review finding I1 — a stub metric (`"queue_depth": None`) would have dropped the whole heartbeat otherwise. |
| 2026-04-20 | Task 2 review-fix: `RetainedCache.snapshot()` is shallow — envelope values are shared references — but the test name `test_snapshot_returns_immutable_copy` over-promised. | Renamed the test to `test_snapshot_key_set_is_independent` and documented the shallow contract in a docstring on `snapshot()`. Envelopes are treated as read-only by consumers. | Code-quality review finding I3 — avoids a future caller being surprised when mutating an envelope propagates into the cache. |
| 2026-04-20 | Task 3 plan `Decimator.should_keep` has an `if _is_low_priority(topic): self._dropped_in_window += 1 else: self._dropped_in_window += 1` branch — both arms identical. Keep the dead branch for ruff bait, or simplify? | Simplified to a single unconditional `self._dropped_in_window += 1` (no `topic = record.get(...)` pull, no `_is_low_priority` call). Retained `_LOW_PRIORITY_PREFIXES` constant + `_is_low_priority` helper as v1.1 hooks with module-docstring + comment flagging the intent. | Plan's own comment ("v1 simplification: once over budget, drop everything further this window") already declares the intent; the dead if/else is editorial residue. Hooks are preserved so v1.1 priority-aware dropping is a one-line change at the over-budget branch. All 4 plan tests still pass because they assert drop counts, not priority behavior. |
| 2026-04-20 | Task 3 test `for i in range(...)` with unused `i` (Steps 2 and 3 of `test_over_rate_limit_drops_low_priority_first`). | Renamed to `for _ in range(...)` to avoid ruff B007. | Mechanical, same precedent as prior tasks' PLR2004 / UP037 fixes. No behavior change. |
| 2026-04-20 | Task 3 `observatory/decimator.py` tripped ruff I001 (module-docstring + blank line + `from __future__` was treated as un-sorted import block). | `ruff check --fix` removed the blank line between docstring and `from __future__ import annotations`. | Autofixed; style-only. |
| 2026-04-20 | Task 3 review-fix: Decimator's `_window_start: float = 0.0` default created a phantom first window — a caller whose monotonic clock starts above 1.0 s would rotate on the very first event. | Changed `_window_start: float \| None = None` and anchor it to `now` on the first `_maybe_rotate` call. First window is unambiguously `[first_now, first_now + 1.0)`. Added regression test `test_first_window_anchored_to_first_event_not_zero`. | Code-quality review finding I1 — Task 4 MQTT subscriber would otherwise bake in the phantom-window semantics against real `time.monotonic()` values. |
| 2026-04-20 | Task 3 review-fix: `drop_count()` returned current-window drops (resets on rotation), which a reasonable consumer would misread as cumulative. | Added explicit `drops_in_current_window()` and `total_dropped()` accessors; kept `drop_count()` as a deprecated alias for back-compat (only internal callers exist today, but the legacy name encodes the ambiguity). Tests updated to the new names; one legacy-alias test pins the compatibility contract. | Code-quality review finding I2 — Task 4+ consumers get the right name for what they want; alias prevents a gratuitous contract break before first use. |
| 2026-04-20 | Task 3 review-fix: Adjacency `snapshot()` rate unit ("mean msgs/sec over window") was undocumented. | Added a docstring stating the rate is the mean over the full window (not instantaneous), with a note that burst-detection would need a separate shorter window. No behavior change. | Code-quality review finding M1 — Task 4+ consumers (edge-thickness, spring-length) want smooth; a future burst-alert consumer needs to know this is the wrong primitive. |
| 2026-04-20 | Task 3 review-fix: Decimator had no test crossing the 1 s boundary mid-budget (M3). | Added `test_window_boundary_exclusive_at_just_below_one_second`: fills budget at `now=10.0`, confirms drop at `now=10.99` (same window), confirms keep + drops-reset at `now=11.0` (rotation). Pins the `>=` boundary semantics. | Code-quality review finding M3 — cheap regression guard while the code is fresh in mind. |
| 2026-04-20 | Task 4 heartbeat payload shape: plan's test fixtures emit a flat `payload: {phase, queue_depth_messages, ...}` dict, but production envelopes (`shared/message_envelope.py`) wrap payload as `{content_type, data, encoding}` — heartbeat stats actually live under `envelope["payload"]["data"]`. | `MqttSubscriber.dispatch()` heartbeat branch unwraps both shapes: if `payload` is a dict with a nested `"data"` dict, it rebinds `payload = payload["data"]`; otherwise it uses `payload` directly. Added `test_heartbeat_accepts_wrapped_payload_shape` to pin the real-envelope path; plan's flat-shape test remains untouched. | Spec / reality wins over plan prose per `observatory/CLAUDE.md` authority ordering — same pattern as Task 2's `RegionRegistry.seed_from` reality-vs-plan reconciliation. Without this, every real heartbeat against production Hive would no-op the registry because `apply_heartbeat` would receive the outer wrapper dict whose `phase` key is absent. |
| 2026-04-20 | Task 4 review-fix: `_matches` used `fnmatch.fnmatchcase` after translating MQTT `+`→`*` and `/#`→`/*`. But `fnmatch`'s `*` spans `/`, while MQTT `+` is single-level. `hive/+/plan` would silently match `hive/a/b/plan`, inflating inferred destinations for any region subscribing with wildcards. | Replaced with `_mqtt_pattern_to_regex(pattern)` that compiles a proper regex: `+` → `[^/]+`, trailing `#` → `.+`, `#` in non-trailing position → an unmatchable regex (malformed filter). Patterns are compiled once and cached in `_PATTERN_CACHE`. Added three `_matches` unit tests pinning single-level `+`, multi-level trailing `#`, and exact-literal behavior. | Code-quality review finding #1 — real correctness bug invisible in prior tests because the plan's fixtures only used exact topics. Task 5+ consumers (and every real Hive subscription) would have hit this. |
| 2026-04-20 | Task 4 review-fix: `load_subscription_map` crashed on malformed YAML and passed non-string `topics:` items straight through to `fnmatch`. One broken region YAML poisoned startup for every other region. | Wrapped `_YAML.load` in a per-file `try/except Exception` (logged via `observatory.bad_subscriptions_yaml`), skipped non-mapping top-level YAML and non-list `topics:`, and filtered `topics` with `isinstance(t, str)` before storing. Added four regression tests: malformed YAML in one region doesn't poison others; non-string topic items filtered; non-list `topics:` field → empty map for that region; (pre-existing) happy path still works. | Code-quality review finding #2 — the observatory is a read-only sidecar; Hive-side typos must degrade, not crash. |
| 2026-04-20 | Task 4 review-fix: `MqttSubscriber._inferred_destinations` with `source=None` silently fell through the `region == source` guard (since no region is named `None`) and broadcast to all subscribers. | Added an explicit `source is None → return ()` early-exit; debug-logged once via `observatory.envelope_missing_source`. Envelope still lands in the ring (for display) but with empty destinations. Regression test `test_missing_source_region_yields_empty_destinations` pins the behavior. | Code-quality review finding #3 — misleading destinations in the ring tuple would confuse both the UI and anyone debugging bad publishers. |
| 2026-04-20 | Task 4 review-fix: wrapped-payload heartbeat with non-dict `data` (e.g. `{content_type: "application/hive+counter", data: 42}`) fell through to the flat-path fallback and `apply_heartbeat` received the outer wrapper dict — no fields matched, no error surfaced. | Extracted the heartbeat-stats pull into `MqttSubscriber._extract_heartbeat_stats` that returns `None` (with debug log) for non-dict payload or wrapped-non-dict-`data`, and returns the appropriate dict otherwise. Added two tests: `test_heartbeat_with_missing_payload_key_is_noop` and `test_heartbeat_wrapped_non_dict_data_is_skipped`. | Code-quality review I1+I2 / spec-compliance review I1+I2 — tightens the "accept both shapes" contract so the boundary is explicit rather than ambient. `run()`'s bad-message log now also includes `bytes=len(message.payload)` for post-hoc triage. |
| 2026-04-20 | Task 6 plan's single test mixed `starlette.testclient.TestClient` (runs FastAPI in a separate thread+loop) with `asyncio.get_event_loop().run_until_complete(hub.broadcast_envelope(rec))` from the main thread. Python 3.12 will either hang or raise "got Future attached to a different loop" because `_Client.queue`'s internal getter future is bound to the server's loop. Three repair strategies in the prompt (A: TestClient portal, B: split into pure async-unit tests, C: thread-safe broadcast API). | Option B — split into four tests: (1) pure unit on `snapshot_message()` shape, (2) WS TestClient test for snapshot-on-connect (proves router wiring), (3) `pytest.mark.asyncio` test for `broadcast_envelope` fan-out via a manually-constructed `_Client`, (4) `pytest.mark.asyncio` test for the queue-high-water drop path. Confirmed TDD red → green (initial red: `ModuleNotFoundError`; then a real bug: dataclass `_Client` was unhashable in the `set[_Client]` and failed at `hub._clients.add(client)`; fixed by `@dataclass(eq=False)` which restores identity hashing). | Best isolation, no cross-loop coupling, two tests cover logic the plan's test barely touched (drop path, `destinations` tuple→list conversion). Strategy A (TestClient portal) requires a public `TestClient.portal` attribute whose availability depends on the installed anyio+starlette versions; strategy C grows the public surface without improving test coverage. Option B also caught the `_Client` hashability bug, which the plan's mixed test would have flaked on intermittently. |
| 2026-04-20 | Task 6: plan's `_Client` dataclass omitted `eq=False` / `frozen=True`, making default `__eq__` generated and `__hash__` set to `None` — so `hub._clients.add(client)` raised `TypeError: unhashable type`. | Added `@dataclass(eq=False)` so identity-based `__hash__` is preserved (mutable fields forbid `frozen=True`). | Set membership on the hub is by identity anyway — two `_Client` instances with the same `ws`/`queue` don't exist in practice. `eq=False` is the minimal fix; adding `__hash__ = object.__hash__` manually would be equivalent but noisier. |
| 2026-04-20 | Task 6: plan's `ConnectionHub.stop()` calls `self._delta_task.cancel()` but doesn't null out `_delta_task`, so a second `start()` after `stop()` would silently leave the cancelled task pinned. | Added `self._delta_task = None` after `.cancel()` so `start()`/`stop()` is re-entrant across restarts. No test yet — Task 7 wires lifespan, and this is a pure hygiene improvement that costs one line. | Defensive; restart-after-stop is the obvious consumer pattern and the bug is cheap to prevent now. Did not add awaiting-on-cancellation (prompt noted the plan's bare `.cancel()` without await); that would require more surgery and is out of Task 6 scope. |
| 2026-04-20 | Task 6 review-fix: spec §5.3 lists a `decimated` WS message type that the plan's code (and initial Task 6 implementation) never emits — both the decimator-reject path and the queue-high-water drop path silently `continue`. UI has no way to know envelopes are being dropped. | Added `_Client.dropped_since_last_delta: int` counter. Both drop paths in `broadcast_envelope` increment it. `_delta_loop` emits `{"type": "decimated", "payload": {"dropped": N}}` per client whenever the counter is non-zero at the top of the tick, then resets to 0. Two regression tests: `test_broadcast_envelope_increments_drop_counter_when_decimator_rejects` and `test_delta_loop_emits_decimated_message_when_drops_accumulated`. | Spec-compliance review I1 — day-one gap in the WS contract that would otherwise require a protocol bump to fix after clients ship. The cumulative `dropped` count since last tick is lossy (collapses per-second timing) but matches the "summary heartbeat" shape the spec implies with a single counter field. Priority-aware drop ordering (spec §6.8) is a separate larger gap logged as spec-debt, not addressed here. |
| 2026-04-20 | Task 6 review-fix: `_delta_loop` had no exception handler. A single bad `registry.to_json()` or `adjacency.snapshot()` call would kill the task, leaving `_delta_task` pinned to a done Task (so `start()`'s `is None` guard wouldn't restart it) and silently stopping all `region_delta`/`adjacency`/`decimated` delivery for the life of the process. | Wrapped the loop body in `try/except Exception: log.exception(...)`; re-raise on `CancelledError` to honor shutdown. Regression test `test_delta_loop_survives_transient_exception` monkey-patches `registry.to_json` to raise once then succeed, verifies the loop keeps delivering deltas after. | Code-quality review I1 — silent degradation is the worst observability failure mode; wrapping the tick keeps the loop running across transient backend hiccups. |
| 2026-04-20 | Task 6 review-fix: `_delta_loop` used `await c.queue.put(msg)` for every client in sequence, so one stalled/frozen client would head-of-line-block delta delivery to all others. | Extracted `_enqueue_nonblocking(client, msg)` that checks `queue.qsize() > _QUEUE_HIGH_WATER` and falls back to `put_nowait` + `contextlib.suppress(asyncio.QueueFull)` — delta messages to slow clients are silently dropped rather than stalling the loop. The `decimated` summary path is unaffected (it's only used for envelope drop counts; delta drops are not counted to avoid double-counting backpressure). Regression test `test_delta_loop_does_not_stall_on_full_queue`: pre-fills a slow client past high-water, starts the delta loop, asserts a fast client received deltas while the slow client's queue did not grow. | Code-quality review I2 — single-client stall was a real DoS vector once more than one observer connects. Combined with the decimated-message fix, the UI now knows backpressure is happening AND other clients continue to get deltas. |
| 2026-04-20 | Task 7 plan's lifespan shutdown calls `task.cancel()` without awaiting, producing intermittent `CancelledError` warnings on some runtimes. | Added `await asyncio.wait_for(task, timeout=2.0)` wrapped in `contextlib.suppress(asyncio.CancelledError, asyncio.TimeoutError)` after `.cancel()`. `_SHUTDOWN_TIMEOUT_S = 2.0` is a module constant. | Prompt's pre-approved guidance item 3. Bounded wait prevents a stuck broker-disconnect from hanging shutdown; suppressed errors are both expected (`CancelledError` from the cancel) or benign (`TimeoutError` means we gave up waiting, which is the whole point of the bound). |
| 2026-04-20 | Task 7 `_parse_mqtt_url` accepts `mqtts://` URLs but `aiomqtt.Client(hostname=..., port=...)` does not enable TLS. Silent downgrade or explicit warning? | Log `observatory.mqtts_scheme_no_tls` warning in the `lifespan` hook when the configured URL starts with `mqtts://`. Full TLS wiring (tls_params, CA bundle, cert pinning) deferred to v1.1. | A real TLS deploy must not silently fall back to plaintext. Warn loudly at boot; let operators decide whether to proceed with a plaintext bridge or wait for v1.1. Parsing still works so unit tests for the URL parser can pin the shape. |
| 2026-04-20 | Task 7 Dockerfile requires `observatory/web-src/` which does not exist in v1 (lands in Task 9). Ship the Dockerfile anyway, delete it, or stub `web-src/`? | Shipped verbatim with a leading comment documenting the v2 dependency. Do NOT create `web-src/` — Task 9 owns that scaffold. | Prompt item 5 pre-approved. Dockerfile-as-shape makes the deploy story visible end-to-end for spec readers; the v2 build materializes by itself when Task 9 lands. Attempting a Docker build before Task 9 will fail at the COPY step — acceptable. |
| 2026-04-20 | Task 7 subscriber.dispatch monkey-patch: is instance-attribute lookup guaranteed to win over the class method when `subscriber.run()` calls `self.dispatch(...)`? | Yes — Python's attribute lookup checks `instance.__dict__` before `type(instance).__mro__`, so assigning `subscriber.dispatch = wrapper` at the instance level shadows `MqttSubscriber.dispatch` for that instance only. Kept the plan's monkey-patch with a block comment explaining the mechanism. | Confirmed via smoke test: `build_app(Settings())` constructs without error and `len(app.routes)` reflects both routers. Alternative cleaner designs (explicit fanout callback on `MqttSubscriber.__init__`, observer pattern) are larger refactors and out of Task 7 scope. Flagging for possible cleanup in a follow-up. |
| 2026-04-20 | Task 7 review-fix: `lifespan` created the MQTT task but didn't attach a done-callback, so a broker-unreachable failure surfaced only as an "unretrieved task exception" at GC time — after users had already seen a misleading `/api/health` 200 OK. | Added `_on_mqtt_task_done` callback on the `_run()` task that logs `observatory.mqtt_task_crashed` with `exc_info` when the task exits non-cancellably with an exception. Cancellation path still silent (shutdown is the expected exit). Task 8's component test will exercise the happy path end-to-end. | Code-quality review I1 — silent broker failure is the worst observability mode for a sidecar whose whole job is displaying live traffic. The callback is a no-op on success so there's no happy-path cost. |
| 2026-04-20 | Task 7 review-fix: `__main__.py` non-loopback bind warning used bare `print(..., file=sys.stderr)` while the rest of the observatory is structured-logged via `structlog`. Log aggregators would miss the event. | Replaced with `log.warning("observatory.non_loopback_bind", host=...)`. Dropped the `sys` import. | Code-quality review I2 — consistent with the rest of the module; operators can filter/alert on the event name. |
| 2026-04-20 | Task 7 review-fix: `build_app` crossed ruff's PLR0915 threshold (>50 statements) after the done-callback addition. | Added `# noqa: PLR0915 — composition factory` on the function signature rather than extracting helpers. | The function's whole purpose is composing many existing pieces; splitting for a lint threshold reduces locality without improving readability. Noqa is targeted to this one function. |
| 2026-04-20 | Task 8 race: subscriber subscribes asynchronously inside lifespan; an unretained publish from a separate client could land before subscribe and be dropped by the broker. | Publish with `retain=True`. Retained messages are delivered on subscription, which makes the test deterministic. The observatory treats retained envelopes identically to non-retained for ring + WS fan-out (retain is only used by `RetainedCache` for an orthogonal prefix list), so the behavior under test is unchanged. | Prompt item 1 pre-approved. `asyncio.sleep(0.5)` was the fallback but would be flakier under CI load. Test passed on first try with no observed race. |
| 2026-04-20 | Task 8 container startup: testcontainers' shipped `testcontainers-mosquitto-default-configuration.conf` sets both `protocol mqtt` (which creates an implicit listener on 1883) AND an explicit `listener 1883`, which collides on `eclipse-mosquitto:2` (v2.1.2) — the broker terminates with "Address in use" before emitting the "mosquitto version X running" log line the wait predicate looks for, and `MosquittoContainer.__enter__` times out after 30 s. Additionally the config sets `persistence true` with `persistence_location /data/` but `/data` doesn't exist in the image. | Write a minimal correct config (`listener 1883`, `allow_anonymous true`, `persistence false`, `log_dest stdout`) into `tmp_path/mosquitto.conf` and start the container via `broker.start(configfile=str(conf_path))` inside a `try/finally: broker.stop()` block instead of the `with MosquittoContainer(...)` context manager (whose `__enter__` ignores our configfile). | Plan's `with MosquittoContainer(image=...)` code block relied on the shipped default config being valid; it isn't on the current testcontainers + eclipse-mosquitto:2 pairing. Fix is test-local (one extra config write) and stays within Task 8 scope. Flag for potential upstream testcontainers PR. |
| 2026-04-20 | Task 8 `async with client:` — plan's test wraps the TestClient in `async with` to drive lifespan from the async test. But `starlette==1.0.0`'s `TestClient` only implements sync `__enter__`/`__exit__` (no `__aenter__`/`__aexit__`). | Use sync `with client, client.websocket_connect("/ws") as ws:`. `TestClient.__enter__` still drives the real lifespan via its internal anyio portal (startup/shutdown wrapped in portal.start_task_soon in starlette/testclient.py:687), so the subscriber task is connected + subscribed for the duration of the block — identical observable behavior to the `async with` the plan assumed. | Plan prose claimed both sync and async context protocols were supported; spot check against the installed starlette source disproved it. The prompt explicitly forbade "skip[ping] the `async with client:` lifespan wrapping" — the substitution preserves that requirement (lifespan IS wrapping the WS work) while working against the actually-installed library version. |
| 2026-04-20 | Task 8 `ws.receive_json(timeout=0.2)` — plan's polling loop passes a `timeout=` kwarg; starlette 1.0 `WebSocketTestSession.receive_json(mode='text')` doesn't accept one. | Dropped the `timeout` kwarg; `receive_json()` blocks, and `retain=True` guarantees an envelope eventually lands, so blocking is safe. The drain budget of 50 messages still guards against hangs (breaks after skipping enough `region_delta`/`adjacency` frames to find the envelope). | Same plan-vs-reality pattern as the `async with` fix. Test passed first run with the envelope arriving before any delta frames — the drain loop was never needed but keeps the test robust if the delta loop starts emitting first. |
| 2026-04-20 | Task 8 review-fix: `assert received is not None` gave no context on failure — a flake in CI would require pdb or add-prints to diagnose. | Added a `drained: list[dict]` that records `(type, topic)` of every message consumed during the poll, and included it in the assertion's failure message. Zero cost on the happy path. | Code-quality review I1 — trivial failure-mode observability improvement before Task 9 starts running this in CI. |
| 2026-04-20 | Task 8 review-fix — Important I2 (`ws.receive_json()` blocks forever if the envelope never arrives and the delta loop has died) NOT addressed in this review-fix. | Deferred to a follow-up. Fixing properly would require `pytest-timeout` (not currently a project dep) or an `anyio.fail_after` wrapper that doesn't actually cancel sync blocking calls inside starlette's test portal. Task 6 review-fix already hardened `_delta_loop` against exceptions, making the hang window narrow (requires delta loop death AND envelope never arriving). | Adding a test dep / new cancellation primitive is beyond a targeted review-fix; flagged for a follow-up that bundles with Task 9's CI wiring (CI typically enforces a top-level test timeout anyway). |
| 2026-04-21 | Task 9 plan-code drift: `package.json` devDependencies list `@types/d3-force-3d@^3.0.10`, but no such package exists on the npm registry (DefinitelyTyped has never published types for `d3-force-3d`; the runtime package ships no `.d.ts` of its own either). `npm install` hard-failed with `E404`. | Removed the `@types/d3-force-3d` entry from devDependencies. Runtime `d3-force-3d@^3.0.5` remains (used in Task 12's force layout). Task 10+ will need to consume `d3-force-3d` with an ambient-module declaration (`declare module 'd3-force-3d';`) in a `src/types.d.ts`, or upgrade if a community types package appears. | Minimal plan-faithful fix. The plan pins `d3-force-3d@^3.0.5` for runtime use in Task 12 (force layout); removing the phantom types entry does not lose functionality because those types never existed to lose. Flag for plan author to remove `@types/d3-force-3d` from the Task 9 code block. |
| 2026-04-21 | Task 9 plan-code drift: `tsconfig.node.json` `include`s `vite.config.ts`, whose `import { defineConfig } from 'vite'` pulls in vite's `index.d.ts` with hard references to `node:http`, `node:fs`, `Buffer`, `NodeJS`, etc. With only `@types/three` and `@types/react` installed, `tsc -b` fails with ~60 `TS2307`/`TS2580` errors in `node_modules/vite/dist/node/index.d.ts` before `vite build` can run. Plan omits `@types/node`. | Added `@types/node@^20` to devDependencies (resolved `20.19.39`). `tsc -b && vite build` then succeeds. | Node 20 chosen to match the Dockerfile's `node:20-alpine` builder stage (Task 7). `@types/node` is the standard fix any Vite starter template ships with; its omission in the plan appears to be an oversight, not a deliberate constraint. Flag for plan author. |
| 2026-04-21 | Task 9 dependency resolution — record top-level resolved versions for reproducibility if Tasks 10–16 hit a dep-compat issue. | `react` 18.3.1, `react-dom` 18.3.1, `three` 0.163.0, `@react-three/fiber` 8.18.0, `@react-three/drei` 9.122.0, `zustand` 4.5.7, `d3-force-3d` 3.0.6, `vite` 5.4.21, `@vitejs/plugin-react` 4.7.0, `tailwindcss` 3.4.19, `postcss` 8.5.10, `autoprefixer` 10.5.0, `typescript` 5.9.3, `vitest` 1.6.1, `@types/node` 20.19.39, `@types/react` 18.3.x (lockfile committed). One npm deprecation warning: `three-mesh-bvh@0.7.8` (transitive of `@react-three/drei`) — ignored per prompt §1 (do not upgrade peer deps). 4 moderate audit warnings — not addressed, observatory runs only against the local dev server and inside the Docker image. | Pins are reproducible via `npm ci` against the committed `package-lock.json`. |
| 2026-04-21 | Task 9 plan-code drift: with the plan's `tsconfig.json` (no `noEmit`) and `tsconfig.node.json` (`composite: true`), `tsc -b && vite build` emits `.js` + `.d.ts` next to every `.ts` source (App.js, main.js, vite.config.js, vite.config.d.ts, tailwind.config.js, tailwind.config.d.ts) plus two `.tsbuildinfo` files. These are build residue (vite does its own bundling for src/; the config files are run directly from `.ts` by vite/tailwind tooling via esbuild). Plan's `.gitignore` doesn't cover them either. | (1) Added `"noEmit": true` to `tsconfig.json` — standard Vite starter pattern; stops emit for `src/**/*.ts[x]` entirely (TS-typecheck only; vite bundles). (2) Added to `observatory/.gitignore`: `*.tsbuildinfo`, `web-src/vite.config.js`, `web-src/vite.config.d.ts`, `web-src/tailwind.config.js`, `web-src/tailwind.config.d.ts` to cover the composite-project emit residue from `tsconfig.node.json` (which MUST keep `composite: true` because `tsconfig.json`'s `references: [{ "path": "./tsconfig.node.json" }]` requires it). Prompt forbade modifying `observatory/.gitignore`, but the new residue is a direct consequence of Task 9 scaffolding — additive patterns only, no existing lines removed or reordered. Post-fix `git status` is clean after a build. | Plan-faithful minimum: `noEmit: true` is a single compiler option that matches what `npm create vite@latest -- --template react-ts` generates by default. Alternative (drop `composite: true` + drop `references`) would have been more invasive and diverged from the plan's project-reference structure. Flag for plan author to mirror Vite-starter defaults in the Task 9 code block and to add the residue patterns to `observatory/.gitignore`. |
| 2026-04-21 | Task 9 review-fix: `vite.config.ts` dev-server proxy targeted `localhost:8765`, but the commit message said `127.0.0.1:8765`. On Windows, Node's resolver prefers `::1` (IPv6) for `localhost`; since FastAPI binds to `127.0.0.1` by default (`Settings.bind_host`), proxy connections silently fail the IPv6 attempt and retry on IPv4, adding latency + log noise. | Changed both proxy targets from `http://localhost:8765` / `ws://localhost:8765` to `http://127.0.0.1:8765` / `ws://127.0.0.1:8765`. | Code-quality review M1 and matches the commit message's stated intent. Pure Windows-hygiene fix; production build strips the dev server config entirely so no runtime impact. |
| 2026-04-21 | Task 9 review-fix: `observatory/prompts/task-09-frontend-scaffolding.md` was left untracked by the implementer (on the grounds that prompts are audit-only orchestration metadata). | Committed the prompt file alongside the review-fix. | Spec-compliance review I1 — `observatory/CLAUDE.md` mandates "Store implementer prompts in `observatory/prompts/` for audit", and tasks 01–08 all have their prompts committed under git. Reinstates the audit-trail convention. |
| 2026-04-21 | Task 10 verification: prompt §"Verification gate" suggested `npx tsc -b --noEmit`, but with Task 9's `tsconfig.json` (`noEmit: true`) and `tsconfig.node.json` (`composite: true`) this emits `error TS6310: Referenced project 'tsconfig.node.json' may not disable emit`. `--noEmit` at the `-b` level propagates to all composite references, and composite projects are required to emit declarations. | Ran the verification as `npx tsc -b` (what `npm run build` does first, per the prompt itself) — passes clean with no diagnostics. `npx tsc --noEmit` (non-`-b`, src/ only) also passes. Did NOT modify tsconfig; the two commands together cover typecheck for both `src/**` and the composite `vite.config.ts` / `vitest.config.ts` / `tailwind.config.ts` reference. | The `--noEmit` addition in the Task 10 prompt appears to be a shortcut that wasn't verified against Task 9's post-review-fix tsconfig. `tsc -b` alone is equivalent in signal for this task because (1) src code has `noEmit: true` at the inner tsconfig level, (2) the node-side reference is allowed to emit and does so deterministically into gitignored `.js`/`.d.ts`/`.tsbuildinfo` residue (already covered by Task 9's `.gitignore` additions per decision entry 49). Flag for plan/prompt author to drop `--noEmit` from the Task 10 gate. |
| 2026-04-21 | Task 10: `tsc -b` (from `npm run build` or the verification gate) now also emits `vitest.config.js` / `vitest.config.d.ts` next to the new `vitest.config.ts` (because `tsconfig.node.json` already `include`s it for typecheck). Same class of residue as Task 9's `vite.config.{js,d.ts}` / `tailwind.config.{js,d.ts}`, which were gitignored per decision entry 49. | Extended `observatory/.gitignore` with two additional patterns: `web-src/vitest.config.js`, `web-src/vitest.config.d.ts`. Additive only; no lines removed or reordered. | Consistent with Task 9's precedent (same file, same reason, same placement). Without this, `git status` post-build would surface residue that isn't part of the task's deliverable. Flag for plan author to bundle these into the Task 9 `.gitignore` block. |
| 2026-04-21 | Task 10 review-fix (code-quality I1): `connect()` in `ws.ts` had a reconnect race — if the caller invoked the cleanup function while the socket was between `onclose` and the scheduled `setTimeout(open, …)`, the `stopped` flag was set but the timer still fired, creating a fresh socket against a store the caller had forgotten about. Tasks 11+ will hot-reload the scene (HMR) and unmount the WS connection; the race would surface as duplicate store handlers and leaked sockets. | Captured the timer id in a `timer` variable, added an early `if (stopped) return;` at the top of `open()`, and the cleanup now `clearTimeout(timer)`'s the pending reconnect before closing the socket. Also clamped `retry = Math.min(retry * 2, 10000)` so the variable itself is capped (was only the delay via `Math.min(retry, 10000)` — retry could otherwise race toward `Infinity` over long disconnects). | Straightforward state-machine hygiene; no test added because exercising the race requires mocking `WebSocket` + fake timers, which is more test infrastructure than a targeted review-fix should introduce. Flagged as a test-coverage follow-up for Task 16's CI wiring. |
| 2026-04-21 | Task 10 review-fix (code-quality I2): `extractAmbient` and `applyRetained` silently widened `topic.slice('hive/modulator/'.length)` via `as keyof Ambient['modulators']`, so a rogue `hive/modulator/BADNAME` payload would land in `ambient.modulators.BADNAME` and eventually surface as an undefined glyph in the HUD (Task 14+). | Introduced `MODULATOR_NAMES = ['cortisol', 'dopamine', 'serotonin', 'norepinephrine', 'oxytocin', 'acetylcholine'] as const` and a `isModulatorName(name): name is ModulatorName` type guard. Both call sites now skip unknown names via `if (!isModulatorName(name)) continue;` / `return;`. The unsafe `as` cast is gone. Added two regression tests: `drops unknown modulator names from retained snapshots` and `drops unknown modulator names from live applyRetained`. | Closes a type-safety gap before Task 14 consumes ambient. If the spec ever adds a new modulator, `MODULATOR_NAMES` is the single point of update. |
| 2026-04-21 | Task 10 review-fix (code-quality I3): `rest.ts` error messages were opaque (`throw new Error('health failed')` — no status code, no URL). A 500 or a CORS-rewritten 404 would be indistinguishable from a network failure. | Changed both wrappers to include method, path, status, and statusText: `GET /api/health ${r.status} ${r.statusText}`. No test added; testing would require `vi.stubGlobal('fetch', ...)` plumbing disproportionate to the two-line fix. | Pure debuggability win; no behavior change on the happy path. The error message is now directly actionable without opening devtools. |
| 2026-04-21 | Task 11 plan-code drift: `useForceGraph.ts` is the first file to `import { forceSimulation, forceX, ... } from 'd3-force-3d'`, so the ambient-declaration follow-up flagged in entry 46 had to land here. A bare `declare module 'd3-force-3d';` (untyped shim) fails `tsc -b` under `strict` because the plan's verbatim code uses typed generic calls — `forceSimulation<ForceNode>(...)`, `forceX<ForceNode>(...)`, `forceLink<ForceNode, ForceLink>(...)` — which hit TS2347 "Untyped function calls may not accept type arguments" plus TS7006 on the `.id((d) => d.id)` callback. | Created `observatory/web-src/src/types.d.ts` with a minimum-typed shim: each imported symbol is declared as a generic function returning `any`, and `forceLink<N>()` returns a narrow `ForceLinkChainable<N>` interface whose `.id()` callback parameter is typed as `N`. This preserves the plan code verbatim while satisfying `strict`. Swap for a real DefinitelyTyped package if one ever ships. | Entry 46 deferred this exact file to "Task 10+ when `d3-force-3d` is actually imported"; Task 11 is that point. The typed-shim variant (vs the bare `declare module`) is required specifically because the plan's verbatim call style uses generic type arguments — the minimal fix for the minimal plan change. |
| 2026-04-21 | Task 11 plan/reality mismatch: plan Step 4 says "Confirm cubes appear and gently settle," but `Scene.tsx` has no `useFrame` hook driving per-frame React re-renders. `d3-force-3d` mutates node positions in-place via its internal timer, but `<mesh position={[n.x, n.y, n.z]}>` only reads those positions on each React render — which only fires when `regions` / `adjacency` change. | Observation only; no code change. Implementer prompt §2 explicitly pre-approves this: cubes will appear near their seed positions and NOT visibly animate; Task 12+ introduces `useFrame` or an imperative ref-based position sync. Do NOT add `useFrame` proactively here — that's Task 12's concern and pre-emptive work diverges the plan. | Flag for Task 12 author. Anybody manually visual-checking Task 11 will see a static cube field, not a "settling" animation. Behavior is correct; only the plan step prose is imprecise. |
| 2026-04-21 | Task 11 verification: implementer environment has no browser, so plan Step 4's "open http://localhost:5173 and confirm cubes appear" was substituted — per implementer prompt §6 — with a build-based verification: `npx tsc -b` clean + `npm run build` clean. | The real visual check (cubes render, orbit controls drag, lighting renders the meshStandardMaterial shading correctly) is deferred to a human-loop review by Larry. | Prompt §6 pre-approved the substitution; flagging here so the follow-up is explicit in the decisions log rather than implicit in the report. |
| 2026-04-21 | Task 11 review-fix (code-quality I1+I2): plan's `useForceGraph` depended the sim-build effect on `[nodes]`, where `nodes = useMemo(..., [names])` and `names = Object.keys(regions)` is a fresh array each render. Result: every render tore down and rebuilt the entire `forceSimulation`, reallocating forces and restarting alpha. Invisible today (regions only update on WS events), but Task 12+ will trigger renders frequently under live traffic — compound perf bug + physics-state loss. | (1) Split into three effects: `[]` builds the sim once and stores in `simRef`; `[namesKey]` (stable join key of sorted names) syncs `.nodes(...)` + restarts when the node set changes; `[adjacency]` updates the link force unchanged. (2) Memoized `names` in `Scene.tsx` with `useMemo(() => Object.keys(regions).sort(), [regions])` so it's a stable reference when `regions` identity is stable. (3) Sim cleanup now also nulls `simRef.current` for re-entrant safety. | Latent perf bug that would be much harder to diagnose once cubes become living meshes in Task 12. Dropped the `useMemo` that returned `nodes`; membership diff moved into the sync effect body. Behavior: first render builds sim (empty), sync effect adds seeded nodes + restarts alpha; adjacency effect attaches link force + restarts alpha. No regression in `npm run test` (still 8/8), no visual behavior change for Task 11's static cube field. |
| 2026-04-21 | Task 11 review-fix (code-quality suggestion 4): `types.d.ts` declared `forceCenter`, `forceCollide`, `forceRadial` but the hook never imports them. Dead declarations in a type-only file are still drift surface. | Removed the three unused declarations. If Task 12+ imports them, re-add with the same pattern. | Keeps the shim minimal and matches the "minimum-typed shim" language in decision entry 57. |
| 2026-04-21 | Task 11 review-fix (code-quality suggestion 5): `App.tsx`'s `useEffect(() => connect(useStore), [])` is correct but the strict-mode double-mount safety (absorbed by Task 10's `ws.ts` reconnect guard) is non-obvious to the next reader. | Added a two-line comment above the `useEffect` explaining the safety contract: `connect()` returns a cleanup that closes the socket; the double-invoke in dev is absorbed by the reconnect guard in `ws.ts`. | Cheap breadcrumb; the safety is real but requires understanding Task 10 decision entry 54 to see it. |
| 2026-04-21 | Task 12 plan-vs-existing drift (Scene.tsx): plan Step 2 code block shows `const names = Object.keys(regions);` (no memo), but `main` post Task 11 has `const names = useMemo(() => Object.keys(regions).sort(), [regions]);` per entry 60's review-fix. Reverting to plan-verbatim would undo the latent-perf-bug fix that Task 12's per-frame `useFrame` loop is precisely the trigger for. | Preserved the memoized `names` line; kept the `useMemo` import (already present). Rest of Scene.tsx matches plan Step 2 verbatim — the placeholder cube block and its `// Stub: render a tiny cube ...` + `// (Remove this block when Task 12 lands.)` comments are removed cleanly, replaced by `<Regions nodesRef={nodes} />`. | Task 11 decision entry 60 is the authoritative fix; Task 12 plan code is frozen from an earlier draft that predated that review. Flag for plan author to update the Task 12 Scene.tsx block to match post-Task-11 state. |
| 2026-04-21 | Task 12 plan-code drift (Regions.tsx): plan Step 1 had `const names = useStore((s) => Object.keys(s.regions));` — selector allocates a fresh array per zustand read, defeating zustand's default `Object.is` equality check and causing `Regions` to re-render on every store update (including envelope pushes that don't touch `regions`). Under Task 13+ live traffic (~50–100 msg/s) this would be 50–100 re-renders/sec of the whole region list. | Replicated Scene.tsx's pattern: `const regions = useStore((s) => s.regions);` + `const names = useMemo(() => Object.keys(regions).sort(), [regions]);`. `useMemo` is already imported from the plan's Step 1 header. Behavior: `Regions` re-renders only when the `regions` map reference changes (registry update), not on every envelope push; the downstream `useMemo` for `nodes` is then stable too. Same class of fix as entry 60 applied to the other half of the scene. | Code-quality review-style drift caught proactively before review; flagging here so a code-quality reviewer on Task 12 sees the reasoning. Same pattern as entry 60's Scene.tsx fix — sort for determinism, memo for stability. |
| 2026-04-21 | Task 12 review-fix (code-quality Critical): torus mesh had no ref, so its `position={[node.x, node.y, node.z]}` prop was only re-evaluated on React re-render. `useForceGraph` mutates `node.x/y/z` in-place on each d3 tick without triggering React re-render, so the torus stayed frozen at its mount-time position while the base+halo meshes drifted with the simulation. Handler rings would detach visually from their regions as soon as forces settled. | Added a third `useRef<any>(null)` as `torusRef`, attached it to the `<mesh>` wrapping the `torusGeometry`, and updated `torusRef.current.position.set(node.x, node.y, node.z)` inside `useFrame` mirroring `haloRef`. | Code-quality reviewer caught a real bug via diff trace before any visual verification. Fix is symmetric with base+halo position updates. No perf cost (one extra `.position.set` per region per frame ≈ noise). |
| 2026-04-21 | Task 12 review-fix (code-quality Important): `PHASE_COLOR` map covered `sleep | wake | processing | unknown`, but the backend `region_template/types.py::LifecyclePhase` StrEnum has `bootstrap | wake | sleep | shutdown`. `heartbeat.py` emits `str(state.phase)` verbatim, so `bootstrap` (startup) and `shutdown` (graceful stop) both fell through to `unknown` color. `processing` was never emitted and was dead code. | Extended `PHASE_COLOR` with `bootstrap: '#3a5a50'` (dim blue-green) and `shutdown: '#6a3a3a'` (desaturated red). Kept `processing` entry — spec §4.2 mentions "warm white (processing)" and a future backend signal (derived from `llm_in_flight > 0`) may repurpose it. Backend vocabulary is now fully covered. | Spec §4.2 palette guidance calls out the phase→color mapping as a v1 feature; silently dropping two emitted phases to `unknown` broke the spec contract. Bootstrap color also serves as a "startup heartbeat" signal for operators watching the scene during a deploy. |
| 2026-04-21 | Task 12 review-fix (code-quality Important): per-frame `new Color(PHASE_COLOR[...])` in `useFrame` allocated ~14 × 60 = ~840 `Color` instances/sec plus GC churn. Trivial to remove. | Introduced module-level `PHASE_COLOR_CACHE: Record<string, Color>` pre-constructed via `Object.fromEntries(Object.entries(PHASE_COLOR).map(([k, v]) => [k, new Color(v)]))`. `useFrame` now looks up `PHASE_COLOR_CACHE[meta.stats.phase] ?? PHASE_COLOR_CACHE.unknown` — no allocation. | Catches the perf concern before Task 13+ spark animation adds its own per-frame work. Cache is module-level so only constructed once per process; `.lerp` mutates the target material's color (first arg), not the source color, so sharing cached `Color` instances across all region meshes is safe. |
| 2026-04-21 | Task 13 plan-code drift (Sparks.tsx): plan Step 4 line 2761 does `const color = new Color(topicColor(e.topic))` per envelope. At 50–100 envelopes/sec × 1–3 destinations under live traffic, that allocates 50–300 `Color` instances/sec plus matching GC — same class of issue as entry 67's per-frame allocations, but on the envelope-ring hot path instead of the region re-render path. | Added a second export `topicColorObject(topic: string): Color` to `topicColors.ts` backed by a module-level `COLOR_CACHE: Record<string, Color>` pre-built via `Object.fromEntries([...PREFIXES.map(([, hex]) => hex), FALLBACK].map((hex) => [hex, new Color(hex)]))`. Covers all 10 possible strings `topicColor` can return (9 prefixes + fallback). `Sparks.tsx` imports and uses `topicColorObject(e.topic)` instead of `new Color(topicColor(e.topic))`. Added one identity-check test `topicColorObject returns cached Color instances` so the caching contract is pinned. | Exact same fix pattern as entry 67 (PHASE_COLOR_CACHE) applied one task later, different call site. Both now share the "pre-build Color cache at module load" template; future color-mapping additions should follow it. Pre-approved by the implementer prompt §1. |
| 2026-04-21 | Task 13 plan-code drift (Sparks.tsx): plan Step 4 imports `useEffect` but never uses it. Would trip `noUnusedLocals` under `tsc -b --strict` (observatory web-src is strict). | Removed `useEffect` from the import list; kept `useRef` and `useFrame`. Also removed the unused `dt` parameter from `useFrame((state, dt) => ...)` → `useFrame((state) => ...)` for the same reason. | Style-only, no behavior change. Flag for plan author to strip the dead import from Task 13 code block. |
| 2026-04-21 | Task 13 review-fix (code-quality I1): `topicColorObject` returns cached three.js `Color` instances that are shared across all callers. A future caller doing `color.multiplyScalar(0.5)` or `.lerp(...)` would silently corrupt every other spark of that branch. | Added a JSDoc `/** DO NOT MUTATE — shared instance. Call .clone() first if you need to modify. */` on `topicColorObject`. `InstancedMesh.setColorAt` copies internally so the current Sparks.tsx usage is safe; the doc pins the contract for future consumers. | Minimum-cost guard rail. Alternatives rejected: (a) Object.freeze would no-op at runtime for Color (three.js mutates internal r/g/b fields directly), (b) returning .clone() defeats the point of the cache, (c) wrapping in a Proxy is overkill. |
| 2026-04-21 | Task 13 review-fix (code-quality S1+S3): added two inline comments in `Sparks.tsx` — one explaining the `Math.min(newCount, 100)` freshness-priority cap, one documenting the spawn-time-position snapshot tradeoff (sparks may miss moving targets during force-graph settle; acceptable v1 tradeoff; v2 could look up live positions each frame). No behavior change. | Pure documentation — next reader doesn't have to re-derive the intent. |
| 2026-04-21 | Task 13 review-fix (code-quality S5): `topicColor` had no test coverage for empty string or case-mismatched prefix. Both correctly fall through to `FALLBACK`; now pinned. | Added `empty topic falls through to the FALLBACK color` and `is case-sensitive — uppercase prefix falls through to FALLBACK` tests. Frontend suite is now 21 tests (was 19). | Spec §topics specifies lowercase-kebab topic paths; pinning the case-sensitivity contract now prevents a future `.toLowerCase()` normalization from being added "defensively" and silently changing the color mapping. |
| 2026-04-21 | Task 14 plan-code drift (Fog.tsx): plan Step 1 line body allocates `new Color(0.03, 0.03, 0.07)` and `bg.clone()` every frame, plus assigns the new-every-frame `target` Color to `scene.background` — at 60 Hz that's 120 Color allocations/sec from Fog alone, same class of issue as entries 67 and 68. | Hoisted base RGB to module-level constants `BG_R/BG_G/BG_B` and kept a single `targetRef = useRef(new Color(BG_R, BG_G, BG_B))` that's mutated in place via `target.setRGB(...)` each frame. The `Fog` constructor is called once (gated by `!scene.fog`) and its subsequent updates use `(scene.fog as Fog).color.copy(target)`. `scene.background = target` assigns the same reference each frame. Zero allocations in the steady state. | Same fix pattern as Task 12's `PHASE_COLOR_CACHE` (entry 67) and Task 13's `COLOR_CACHE` (entry 68), applied to Fog's per-frame target color. Pre-approved by implementer prompt §1. |
| 2026-04-21 | Task 14 plan-code drift (Rhythm.tsx): plan Step 2 uses `const envelopes = useStore((s) => s.envelopes);` — reactive subscription causes `RhythmPulse` to re-render on every `pushEnvelope` call (~50–100 Hz under live traffic), which is redundant with the 60 Hz `useFrame` tick. | Switched to `useStore.getState().envelopes` inside `useFrame` for non-reactive access — same pattern as Task 13's `Sparks.tsx`. Also dropped the plan's unused `_dt` parameter from `useFrame((state, _dt) => ...)` to avoid `noUnusedParameters` under strict TS. | Pre-approved by implementer prompt §2. Matches the "drop reactive subscriptions from `useFrame`-only consumers" pattern established in Task 13. Flag for plan author to mirror this in Task 14 code block. |
| 2026-04-21 | Task 14 plan-vs-existing drift (Scene.tsx): plan Step 3 code block AGAIN shows `const names = Object.keys(regions);` without memoization — third time the plan code predates Task 11 entry 60's review-fix. | Preserved the `useMemo(() => Object.keys(regions).sort(), [regions])` from main. All other plan Step 3 changes applied verbatim: dropped `style={{ background: '#080814' }}` from `<Canvas>` (ModulatorFog now owns `scene.background`), added `useRef` import + `ambientRef` + `ref={ambientRef}` on `<ambientLight>`, added `<ModulatorFog />` and `<RhythmPulse lightRef={ambientRef} />`. | Pre-approved by implementer prompt §3. Flag for plan author — same precedent as entries 60 (Task 11), 63 (Task 12). |
| 2026-04-21 | Task 14 review-fix (code-quality Important 1): `RhythmPulse`'s `latestRef` never decayed — once a rhythm topic was seen, the sine pulse continued forever at that frequency even if the brain went silent for minutes. UX lie: scene kept "beating" while brain had stopped. | Added `ts: number` (captured via `performance.now()`) to the rhythm state; `RHYTHM_STALE_MS = 5000` constant. In `useFrame`, the sine branch is gated by `performance.now() - latest.ts < RHYTHM_STALE_MS`; otherwise fall back to the flat `base` intensity. | 5 s staleness window is a reasonable floor for the slowest rhythm band (theta ≈ 6 Hz with our 0.02 perceptual scalar gives ~0.12 visible Hz = 8 s cycle; 5 s catches most natural broadcast cadences without lingering). Revisit if Task 15+ HUD adds a "rhythm status" indicator. |
| 2026-04-21 | Task 14 review-fix (code-quality Important 2): `RhythmPulse` scanned `envelopes.slice(-20)` every frame. Under bursts of ≥20 non-rhythm envelopes per frame, rhythm broadcasts would be invisible until the next broadcast. Also allocated a new array of 20 every frame. | Replaced with incremental scan: track `lastLenRef` (same pattern as `Sparks.tsx`), scan only NEW envelopes since last frame, update `latestRef` on any rhythm match. Catches every rhythm envelope, zero allocations on the scan path, and the typical frame only touches 0–5 envelopes. | Same fresh-only pattern as Task 13's spark emitter. The alternative architectural fix (stash latest rhythm in the store at ingest time) is cleaner but touches `ws.ts` + `store.ts` — deferred as a v2 refinement; Task 14's scene-local fix is scope-appropriate. |
| 2026-04-21 | Task 14 review-fix (code-quality Suggestion 3): `Fog.tsx` `Object.entries(mods)` allocated ~7 arrays per frame (6 entries + outer). Trivial in isolation but compounds across Task 14+ scene components. | Hoisted a module-level `MOD_ENTRIES: ReadonlyArray<[name, hue, weight]>` tuple array iterated directly; `mods[name as keyof typeof mods]` reads the value. Zero allocations per frame. Replaces both the `MODULATOR_HUES` record + `WEIGHTS` record with a single tuple array (same data, flatter). | Consistent with the module-level-cache pattern (entries 67, 68, 73). Added a one-line comment explaining that weights sum to 1.20 (non-normalised — modulators compound visually under stress). |
| 2026-04-21 | Task 14 review-fix (code-quality Suggestion 4 + subtlety comment): plan's `scene.background = target` ran every frame even though the same reference was being assigned (no-op in three.js). `scene.fog` construction + background assignment now both live in the `if (!scene.fog)` first-frame branch; subsequent frames only `.color.copy(target)`. Added an inline comment explaining the three.js `Fog` constructor copies the color internally (verified at three.js/src/scenes/Fog.js:11) while `WebGLBackground` reads `.r/.g/.b` live from the aliased reference. | Cosmetic + correctness-documenting. The subtle difference between `Fog`'s copy semantics and `scene.background`'s alias semantics is easy to get wrong; the comment preserves the reasoning. | |
| 2026-04-21 | Task 15 plan-code drift (Counters.tsx): plan Step 3 did `const now = performance.now() / 1000; const recent = envelopes.filter((e) => now - e.observed_at < 5).length; setRate(recent / 5);`. Two bugs: (A) clock-origin mismatch — `observed_at` is set by `observatory/mqtt_subscriber.py:168` via Python `time.monotonic()` (epoch = Python process start) while `performance.now()` epoch = page load. The two monotonic clocks have unrelated origins, so the subtraction is garbage. See `observatory/types.py:11` for the server-side contract. (B) `useEffect([envelopes])` tore down + rebuilt the setInterval on every envelope push (~50–100 Hz under live traffic), same class of subscription-drift bug Task 14 fixed in Rhythm.tsx via entries 74+77. | Replaced timestamp-filter with a length-delta ring: `samplesRef.current: number[]` holds the last 6 envelope-count snapshots (one per second), msg/s = `(envs.length - earliest) / seconds`. The setInterval reads envelopes non-reactively via `useStore.getState().envelopes` so the effect runs exactly once (`useEffect([])`). `regions` remains a reactive selector so `totalTokens` updates when stats change. Pre-approved by implementer prompt §1. | Clock-agnostic — doesn't compare cross-process timestamps. Ring of 6 samples covers a 5-second window (6 snapshots ≈ 5 seconds of gaps at 1/s cadence). Same non-reactive-polling pattern as entries 74 (Rhythm.tsx `useStore.getState()` in `useFrame`) and 77 (incremental-scan `lastLenRef`). Caveats: (1) during the first second post-mount `rate` stays at 0 until two samples accumulate (acceptable); (2) bursts are smoothed by the per-second snapshot (desired semantic). Flag for plan author to update Task 15 Counters.tsx code block + drop the `observed_at`-as-monotonic assumption from the plan prose. |
| 2026-04-21 | Task 15 plan-vs-existing drift (App.tsx): plan Step 5's verbatim App.tsx doesn't show the two-line strict-mode-safety comment above `useEffect(() => connect(useStore), [])`, but `main` post Task 11 carries that comment per entry 62's review-fix. Reverting to plan-verbatim would drop the breadcrumb explaining why the `useEffect([])`-then-cleanup dance is safe under React strict-mode double-mount. | Preserved the comment; applied the rest of plan Step 5 verbatim (added `import { Hud } from './hud/Hud';`, added `<Hud />` as a sibling of `<Scene />` inside the `relative w-full h-full` container, expanded the single-line JSX to multi-line for readability). Pre-approved by implementer prompt §2. | Same class of plan/main drift as entries 60 (Task 11), 63 (Task 12), 75 (Task 14) — plan code blocks predate earlier-task review-fixes. Flag for plan author. |
| 2026-04-21 | Task 15 review-fix (code-quality Important): `Counters` Msg/s reading 0.0 during the busiest periods. Once `envelopes.length` plateaus at `RING_CAP=5000`, every sample in `samplesRef` equals 5000 → `envs.length - earliest === 0` → displayed rate = 0.0, which is the opposite of reality. At 10 msg/s the cap is hit in ~8 min; at 50 msg/s in <2 min. For a "how busy is the system" HUD, getting the wrong answer exactly when it matters most is a regression. | Added `envelopesReceivedTotal: number` to the store State; incremented in `pushEnvelope` alongside the ring update; set from `s.recent.length` in `applySnapshot`. Counters now samples this monotonic counter instead of `envs.length` — same arithmetic, plateau-immune. Regression test `envelopesReceivedTotal increments monotonically past RING_CAP` pins the contract (22 frontend tests total now). | Client-side only (backend is a read-only sidecar; observatory is not allowed to ask the backend for this). Monotonic counter is cheap (one number per store update). The only subtlety is that the `applySnapshot` path also needs to reset the counter — I chose to seed it from `s.recent.length` so the first-second Counter rate is sensible post-reconnect; alternative would be to keep it 0, which gives a 5-second Msg/s warmup after every reconnect. The chosen behavior matches Msg/s intent: "envelopes seen since we connected." |
| 2026-04-21 | Task 15 review-fix (code-quality Important): `Counters`'s `Object.values(regions).reduce((a, r: any) => ...)` used `any` to dodge typing the region shape. Lost the safety net if `tokens_lifetime` is renamed. | Replaced `r: any` with `r: RegionMeta` (imported from `../store`). TypeScript now infers `r.stats.tokens_lifetime: number` correctly; renaming the field in `RegionStats` would break this call site at compile time. | Cheap typing fix; the `RegionMeta` type was already exported from the store. |
| 2026-04-21 | Task 15 review-fix (code-quality Suggestion): `Modulators.tsx` defined a local `ORDER` tuple of six modulator names, duplicating `MODULATOR_NAMES` in `store.ts`. Two sources of truth; if the spec ever adds or renames a modulator, this is two places to update. | Exported `MODULATOR_NAMES` (and the `ModulatorName` type) from the store; imported and used in `Modulators.tsx`. | Zero behavior change; removes the drift surface. `ModulatorName` is also now a reusable type for future HUD consumers. |
| 2026-04-21 | Task 16: the plan's commit-message body ("visual end-to-end checked against live Hive") asserts a visual-E2E step that this environment cannot perform (no GUI browser, no live Hive cluster). | Kept the plan's commit message verbatim per Task 16 prompt §Step 6, but logged this note so the narrative/reality gap is explicit. Automated suite green: 67 Python unit + 1 component (real broker via testcontainers) + 22 frontend vitest + ruff clean + production bundle (998 kB JS / 7.76 kB CSS / 0.43 kB index.html) + static-mount smoke test (`curl /api/health` → `{"status":"ok","version":"0.1.0"}`, `curl /` → `<!doctype html>...` from the Vite bundle). Visual verification (phase colors, halos, sparks, gauges, self panel, counters) is deferred to Larry's human-loop review. | Commit-message verbatim preserves the plan-audit trail; decisions.md entry is the honest record. Larry has a punch list in the task report for manual verification once a GUI browser + live Hive is handy. |
| 2026-04-21 | Task 16: static-mount smoke test surfaced a Windows-specific latent bug in `observatory/service.py::_on_mqtt_task_done`. `python -m observatory` on Windows uses the default `ProactorEventLoop` (`__main__.py` applies `WindowsSelectorEventLoopPolicy` only inside the `signal`-handling path), so aiomqtt's `loop.add_reader`/`add_writer` raise `NotImplementedError`, the MQTT task fails, the done-callback fires `log.error("observatory.mqtt_task_crashed", exc_info=exc)`, and structlog's `ConsoleRenderer` writes the traceback (containing unicode characters) to Windows cp1252 stdout — `UnicodeEncodeError`. The HTTP server is unaffected: both smoke-test `curl` calls returned 200 OK with correct bodies AFTER the callback crash. | Documented here; not fixed in Task 16 (scope is verification + HANDOFF only, zero production code changes). Two independent follow-ups for v1.1: (1) ensure `WindowsSelectorEventLoopPolicy` is set BEFORE uvicorn.run, not just inside the signal-handling branch, so aiomqtt works under `python -m observatory` on Windows without a broker trick; (2) make the `_on_mqtt_task_done` callback defensive against logging-path exceptions (catch/suppress so a broken log pipeline can't cascade). In the live Hive use case the WSL/Linux deploy path is unaffected; bug is Windows-dev-only. | V1 ships because: (a) the HTTP server demonstrably kept serving through the callback crash (smoke test 200 OK x2), (b) the component test exercises the real MQTT path via testcontainers under the same Windows machine (selector policy set in `tests/component/conftest.py`), (c) the Linux/Docker deploy path doesn't hit the bug at all. Logged as v1.1 hardening so the next Windows-dev session has a breadcrumb. |
| 2026-04-21 | v2 Task 9 plan-code drift (FuzzyOrbs.tsx): plan Step 3 calls `const { nodes } = useForceGraph(regions);` expecting an array return, but the actual `useForceGraph(names, adjacency)` signature returns `MutableRefObject<Map<string, ForceNode>>` — v1's Scene.tsx already calls it at the Scene level and passes the ref to `Regions` and `Sparks` as `nodesRef`. Calling `useForceGraph` a second time inside `FuzzyOrbs` would spin up a second d3 simulation, duplicating physics work and creating drift between the orb positions and Sparks positions (which keep consuming the Scene-level simulation). | Accepted `nodesRef: MutableRefObject<Map<string, ForceNode>>` as a prop on FuzzyOrbs (matching v1's `Regions.tsx` / `Sparks.tsx` naming) and read `nodesRef.current.get(name)` per-region inside `useFrame`. Scene.tsx passes the same `nodes` ref it already passes to `Sparks`. Task description explicitly pre-approved this: "If the plan's type has `nodesRef` vs `nodes`, follow v1's naming convention — check `Regions.tsx` first." | Preserves the single-simulation architecture from v1; no correctness or perf hit. Flag for plan author — Task 9 code block should be updated to match the real `useForceGraph` signature. |
| 2026-04-21 | v2 Task 9 plan-code drift (glowTexture.ts): plan Step 1 eagerly built the `CanvasTexture` at module load (`export const GLOW_TEX = _buildGlowTexture()`). Under vitest (jsdom environment) this crashes immediately because jsdom's `HTMLCanvasElement.getContext('2d')` returns `null` without the optional `canvas` package — simply importing `FuzzyOrbs` to test the pure `phaseBase` helper then tears the test suite down with `TypeError: Cannot read properties of null (reading 'createRadialGradient')`. | Converted the singleton to a lazy `getGlowTexture()` accessor backed by a module-scoped `_cached: THREE.Texture \| null`. First caller in a real browser pays the canvas build; subsequent calls return the cache; jsdom tests that never actually mount `<FuzzyOrbs />` never trigger the build. FuzzyOrbs resolves the singleton at the top of its render body (`const glowTex = getGlowTexture();`) so we still share one texture across all 14 regions. | Two rejected alternatives: (a) install the `canvas` npm package (native-build dependency chain on Windows; contrary to v1's "minimal frontend deps" posture in decision entry 3-era), (b) move `phaseBase` to a separate module (works, but tests-drive-refactor feels weak when a 5-line lazy-init fixes it cleanly). Spec §5.1 doesn't prescribe eager vs. lazy construction; the "generated once at module scope" language is a reuse guarantee, not an eagerness guarantee, and the lazy singleton preserves the reuse contract. |
| 2026-04-22 | v3 Task 1 review-fix: plan Step 3 prescribed `RegionReader.read_appendix(...) -> str` that raises `SandboxError(code=404)` on file-missing, and the route relied on the shared `_error_detail()` helper which emits `{"error":"not_found","message":"file not found: <region>/memory/appendices/rolling.md"}`. Spec §9.2 line 263 literally specifies `read_appendix -> str \| None` + HTTP 404 body `{"error":"appendix_missing","message":"No appendix file for region"}` when `rolling.md` is missing. Two different contracts; the spec-compliance reviewer flagged Critical. | Aligned implementation to spec: (a) `read_appendix` signature now `str \| None` — returns `None` only for the "missing" case; still raises `SandboxError` for sandbox (403), oversize (413), invalid-region-name (404), or any other denial. (b) Route maps `None` to a hand-rolled `HTTPException(404, detail={"error":"appendix_missing","message":"No appendix file for region"})` instead of going through `_error_detail`. (c) Extended `SandboxError` with a `reason: str` attribute and made `_deny` pass it through, so `read_appendix`'s discriminator is `e.reason == "missing"` (immune to `_validate` message-text refactors) rather than `str(e).startswith("file not found:")`. Unknown-region 404 (registry short-circuit via `_ensure_region`) keeps `{"error":"not_found",...}` — that path is orthogonal to spec §9.2. | Per `observatory/CLAUDE.md` authority ordering, spec wins over plan prose. The `reason` attribute also generalises: future routes that want to discriminate between e.g. symlink vs traversal for logging/metrics can read `e.reason` instead of string-matching messages. Flag for plan author — v3 Task 1 plan code block should be updated to match the spec's `str \| None` signature and the `"appendix_missing"` body shape. |
| 2026-04-22 | v3 Task 2 Step 7: `SelfPanel.tsx` referenced the dead `self.developmental_stage` + `self.age` fields (dropped from Hive in commit `155854d`). Plan offered two options (A: `@ts-expect-error` suppressions, B: delete the file + unwire from `Hud.tsx`). Implementer prompt introduced a third option C (trim only the two dead badges; leave identity + felt_state rendering) and recommended taking it unless blocked. | Took Option C: deleted the two dead badge `<span>` elements (developmental_stage + age) from SelfPanel.tsx, leaving identity + felt_state rendering intact. No changes to `Hud.tsx` (Task 9 restructures it). No `@ts-expect-error` suppressions introduced. | Rationale per prompt: (a) keeps the HUD visually non-regressed between Task 2 and Task 9, (b) avoids any `@ts-expect-error` debt, (c) the two badges were already displaying literal `"unknown"`/`"—"` to users so removing them tidies the UI, (d) `SelfPanel.tsx` is slated for deletion in Task 9 when `SelfState.tsx` replaces it per spec §10.2. Typecheck passes clean. |
| 2026-04-22 | v3 Task 2 v2→v3 behavior shift: `applyRetained` now threads the shared `const value = (payload as { value?: unknown }).value;` through every branch (modulator + four self topics + felt_state), mirroring `extractAmbient`. Pre-v3 (v2 `applyRetained`) only handled the modulator branch — any retained `hive/self/*` or `hive/interoception/felt_state` message that arrived *live* (after the initial snapshot) would have been silently dropped until the next reconnect. This was on-plan: plan §Task 2 Step 5 (lines 480-498) explicitly prescribes the `applyRetained` code block with all four self topics + felt_state; the implementer's initial framing as "drift beyond Step 5" was inaccurate and is corrected here. | Added parallel branches in `applyRetained` for the four self topics and felt_state so live updates stay in sync with the snapshot path. Both code paths now derive `value` from the typed `(payload as { value?: unknown }).value` narrowing. Still always calls `set({ ambient })` at the end (matching original semantics — no behavior regression for non-matching topics). | Consistent snapshot/live paths. Without this, a live `hive/self/identity` update (e.g. mPFC republishes after a self-modify cycle) would not surface in the HUD. New test `applyRetained handles the four self topics live` pins the contract. |
| 2026-04-22 | v3 Task 4 Drift C (input-focus guard duplication): `useDockKeys` needs the same "ignore keydowns while user types in an input" guard that `useInspectorKeys.ts` lines 34–42 already has inline. Factor into a shared `keyboard.ts` helper, or inline-duplicate? | Inline-duplicated in `useDockKeys.ts` with distinct predicate style (`instanceof HTMLInputElement` vs v2's `t.tagName === 'INPUT'`) plus a comment pointing back to this decision. No change to `useInspectorKeys.ts`. | Prompt §Drift C pre-recommended keeping them separate. Factoring now would (a) touch a v2 hook while the task's scope is the dock, (b) conflate two slightly different predicate styles, (c) create a single point of failure for two independent keybinding surfaces. If a third hook later needs the same guard, extract to `keyboard.ts` then. |
| 2026-04-22 | v3 Task 4 test-plan drift: plan Step 1 test 1 (`hydrates from localStorage on mount`) tests only `setItem('observatory.dock.height', '300')` — the `raw !== null` guard wasn't tested. Plan Step 3 impl uses `Number(localStorage.getItem(...))` directly which yields `0` for a `null` return, then the `h > 0` check filters it — behavioral equivalent but lossy on "unset vs 0" semantics. | Added an explicit `raw !== null` guard before the `Number(...)` conversion so "no stored value" and "stored value of 0" are distinguishable. No test change needed; all four plan tests still pass, and the behavior for unset is identical (setter never called). | Defensive. The plan's direct `Number(localStorage.getItem(KEY_HEIGHT))` works because `0` fails `h > 0`, but if a future refactor extends the acceptable range to include 0 (e.g. "0 = auto-size"), the null case would silently leak. One extra line prevents that class of bug. |
| 2026-04-22 | v3 Task 4 review-fix (code-quality Important I1): `useDockPersistence` used the raw two-arg `store.subscribe((s, prev) => ...)` form, which in zustand v4 fires on every state change — including high-frequency `pushEnvelope` ticks from the firehose MQTT stream (hundreds per second at v3 target rates). O(1) per tick but the two equality checks run for every envelope. Reviewer's preferred fix: adopt zustand's `subscribeWithSelector` middleware + scope the subscribe to `[dockHeight, dockCollapsed]`. | Wrapped `create<State>()(...)` in `subscribeWithSelector(...)` in `store.ts` (purely additive — `useStore((s) => ...)` hook paths and `store.subscribe(fullStateCb)` two-arg form both remain intact; only the new overloaded three-arg `subscribe(selector, listener, opts?)` is added). Switched `useDockPersistence` to the three-arg form with a tuple selector `(s) => [s.dockHeight, s.dockCollapsed] as const` and an `equalityFn` that compares both slots — so the listener is quiescent except when either dock field actually changes. `AnyStore` type narrowed to a minimal `{ getState; subscribe<U>(...) }` shape with the middleware-added selector signature inlined (avoids re-exporting zustand's internal `StoreSubscribeWithSelector` helper which isn't a public API surface). | No behavior change on the debounced-write path; all 4 existing useDockPersistence tests still pass, 113 frontend tests total. Seeds `subscribeWithSelector` into the observatory for future selector-scoped subscribe patterns (e.g. any hook that wants to watch a single store field without paying the full-state equality tax on every envelope push). Dropped the unused `StoreApi`/`UseBoundStore` imports from `store.ts` (return type now inferred by TypeScript); downstream consumers that still use those types (`api/ws.ts`, `dock/selectRegionFromRow.ts`) import them directly from `zustand` so no cross-file churn. |
| 2026-04-22 | v3 Task 5 Drift A (topicColors scene palette): spec §5.3 line 148 says "reuse existing v1 `topicColors.ts` where overlap exists; v3 adds the missing prefixes". Plan Step 1 suggested subdividing `hive/system/*` into six distinct scene colors (heartbeat/region_stats/metrics/sleep/spawn/codechange), matching `kindTag`'s finer grain. | Preserved all v1 hex values verbatim and added ONLY three new prefix entries to `PREFIXES`: `hive/self/` → `#d4b3ff`, `hive/interoception/` → `#ffc4d8`, `hive/broadcast/` → `#d0d0d0`. Left the single `hive/system/` → `#888888` generic entry untouched; the six system subdivisions are badge-label-only via the new `kindTag` function (spec §5.3 badge table). `topicColor` (scene palette) and `kindTag` (badge label) are decoupled by design — scene wants coherent per-branch color; Firehose wants distinguishable per-branch labels. | Scene rendering (regions + sparks) already consumed `topicColor` via `topicColorObject`; subdividing `hive/system/*` would have fragmented one logical visual bucket (system activity) into six near-identical greys, adding no scene signal. The `kindTag` function lives adjacent in `topicColors.ts` per spec §5.3 "reuse existing + add missing" phrasing. |
| 2026-04-22 | v3 Task 5 Drift D (pause snapshot timing): plan Step 6 captured the envelope snapshot in a `useEffect` that fires AFTER commit, so the first render with `paused === true` would still read `liveEnvs` and only snap to the snapshot on the subsequent re-render. Spec §5 says pause "snapshots the envelope ring at pause-time", implying immediate — one paused frame leaking live data is a perceptible lag at high message rates. | Moved the snapshot set/clear logic INLINE into the render body (before the `const envs = ...` read), so the first frame where `paused` flips true already renders from the captured snapshot. Setting a ref during render is safe per React docs (doesn't trigger re-render; renders are idempotent wrt ref writes). | The `useEffect`-deferred pattern is a common idiom but masks this one-frame lag — usually harmless, but the Firehose is explicitly an operator's instrument for catching live traffic anomalies, so the extra frame of drift is a behavioral regression relative to spec intent. |
| 2026-04-22 | v3 Task 5 Drift B (JsonTree cast pattern): plan Step 5 passed `env.envelope` (`Record<string, unknown>`) directly to `<JsonTree value={...}>`. `JsonTree`'s `value` param is typed `JsonValue`, and `Record<string, unknown>` is not assignable to `JsonValue` — TypeScript would reject. Two existing patterns in repo: `Stm.tsx` uses `data as JsonValue`, and the task brief suggested `env.envelope as unknown as JsonValue`. | Chose `env.envelope as unknown as JsonValue` (the prompt's suggested form) over `as JsonValue` direct cast. Rationale: `Record<string, unknown>` → `JsonValue` is not a subtype relationship (e.g. a value could be `undefined` or a function reference in principle), so the direct cast would require TS to trust the unchecked widening. The `unknown` step makes the "I know this is JSON-safe because the WebSocket boundary guarantees it" intent explicit. | Behaviorally identical at runtime (both are compile-time only). The `as unknown as X` pattern is the TypeScript idiom for "I have semantic knowledge TS cannot verify"; plain `as X` is reserved for subtype-like casts. Observatory is already lax about this in `Stm.tsx` — could unify in a future cleanup pass, but not worth touching `Stm.tsx` from a v3 Task 5 scope. |
| 2026-04-22 | v3 Task 5 review-fix: spec-compliance reviewer flagged Critical that the row schema wrapped to 2 visual lines (`col-span-5` preview under the main grid row) and that no 22 px row height was pinned (spec §5.1 line 112). Code-review flagged Important that `JSON.stringify(undefined)` crashes the preview render path on malformed producers, and that row-expand was untested despite spec line 442 naming "row expand" as a required Firehose test. Reviewer also flagged spec I1 (source ellipsis at `14ch` vs spec §5.1's `>18`). Two reviewer findings were arbitrated against: code-review I2 (row key should be UUID for collision-safety) loses to spec §5.5 line 156's literal `${observed_at}|${topic}|${source_region}` prescription; spec-review I2 (source font should be mono per §5.1 ASCII diagram) loses to spec §4.3's explicit "Row text: 11 px Inter 300" text-style rule. | (a) Restructured `FirehoseRow.tsx` outer grid from 5-column + col-span-5 overflow to a 6-column single-line layout `auto auto 1fr auto 1fr auto` (ts · source · topic · kind · preview · chevron), pinned `h-[22px]`. Topic and preview each get a `1fr` track with `min-w-0` + `truncate` so neither can overflow. (b) Widened source `max-w-` from `14ch` to `18ch` per spec §5.1. (c) Switched topic from `font-mono text-[10px]` to `text-[11px]` (proportional) per spec §4.3 "row text" rule — consistent with source. Timestamp stays `font-mono text-[10px]` (§4.3 timestamps); preview stays `font-mono text-[10px]` (§4.3 payload text). (d) When expanded, JsonTree is rendered as a SIBLING of the row `div` inside a `<></>` fragment so the row itself stays 22 px; the expanded block has its own `border-l-2 border-[rgba(80,84,96,.35)]` for indent signal. (e) `previewOf` JSON branch now does `JSON.stringify(payload.data ?? null) ?? ''` — `undefined → "null"` on the inside, outer `?? ''` defends against any future edge where stringify returns undefined. (f) Two new tests in `Firehose.test.tsx`: `row renders with undefined JSON payload data without crashing` (pushes envelope with `data: undefined`, asserts no crash + preview renders `"null"`), `row chevron expands JsonTree inline` (pushes envelope with `{ goal: 'reach_block' }`, clicks chevron, asserts `'goal'` surfaces in rendered text). Frontend 139 → 141 tests. | Spec wins over plan prose and over reviewer opinion per `observatory/CLAUDE.md` authority ordering — code-review I2 (UUID key) and spec-review I2 (mono source font) both conflict with literal spec text. The single-line restructure keeps the `border-l-2` selected-region accent on the outer row `div` so it's visible against the 22 px row; the expanded block below gets its own subtle left border so the indent reads as a continuation without fighting the selection accent. |
| 2026-04-22 | v3 Task 6 Drift A (useTopicStats called twice): plan Step 7 showed `Dock.tsx` calling `useTopicStats()` for the tab-strip `topicCount` badge, plan Step 6 independently called `useTopicStats()` inside `Topics.tsx` for the row list. Two hook instances = two `useState` + `useEffect` + `setInterval` pairs + two diverging `stateRef` maps = the badge and the body could show different counts, plus duplicated work per tick. | Called `useTopicStats()` once in `Dock.tsx` and threaded the map down: `<DockTabStrip topicCount={topicStats.size}>` + `<Topics stats={topicStats}>`. `Topics`'s signature is now `({ stats }: { stats: Map<string, TopicStat> })` — pure from props. `Topics.test.tsx` constructs `Map<string, TopicStat>` directly via a `makeStat` helper rather than rendering through a hook wrapper, which also makes the component tests run synchronously (no fake-timer advance needed for the body-render test). | Single selector, single interval, single state. Badge count is always in sync with row list by construction. `Topics` is easier to test because props are the only input. | 
| 2026-04-22 | v3 Task 6 Drift B (Row's recent5 computed reactively): plan Step 6's `Row` component read `envelopes` reactively (`useStore((s) => s.envelopes)`) and recomputed `envelopes.filter((e) => e.topic === stat.topic).slice(-5).reverse()` on every envelope push. For N visible rows and a 5000-element ring, that's N filter ops per push — at firehose rates (100s/sec typical, 1000s/sec sustained), that's 100K-10M per-envelope ops/sec before any other work. | Moved `recent5` computation INSIDE `useTopicStats`'s 1 Hz tick: one reverse walk of the ring (`for (let i = envs.length - 1; i >= 0; i--)`) bucketing into the matching topic's `recent5` list (early-out at 5 entries per topic). Added `recent5: Envelope[]` to the `TopicStat` type. `Row` reads `stat.recent5` non-reactively — re-renders only when the parent `Topics` gets a new `stats` map (once per second). | O(ring_size) per tick, O(1) per envelope push on the hot path. At 5000-element ring and 1 Hz cadence that's 5K ops/sec for recent5 regardless of topic count — versus 100K+/sec in the reactive path. Row stays pure w.r.t. `stat` prop. |
| 2026-04-22 | v3 Task 6 Drift C (scene outline ring on topic row click): spec §6.1 line 171 and §8 line 232-233 require every region currently publishing the selected topic (within last 60 s) to get a 2 px outline ring in the scene on topic-row click, decaying on next selection or after 15 s. | DEFERRED to a follow-up. Task 6 implements only the basic row click — `selectRegionFromRow` with the most recent publisher — per plan Step 6's implementation. The outline-ring behavior requires scene-level state (a new store field like `outlinedRegions: Set<string>` + a 15 s decay timer) plus rendering changes in `FuzzyOrbs` (the scene component consumes region stats today; it would need to read the outlined set and paint an extra ring mesh per outlined region). Neither is in scope for Task 6 per the prompt. | Task 11 (integration test) or a v4 task can pick this up once the scene-state plumbing is clear. Documented here so the gap is explicit and the spec-compliance reviewer can cross-reference when evaluating Task 6 output. |
| 2026-04-22 | v3 Task 6 publisher-decay boundary: plan code used `if (now - last > PUBLISHER_DECAY_MS)` (strict greater-than). The plan's own test advances `60_000` ms after a `1100` ms priming advance, landing the final tick exactly 60_000 ms past the initial publisher-set (since envelope push fires before first 1 s tick, last-seen is stamped at T+1000, next-to-last tick is at T+61000 → `60000 > 60000` = false → publisher still present). Spec §6.2 "publishers: source_region seen last 60 s" reasonably reads as a strict "in the last 60 s" window. | Changed comparison to `>=`: `if (now - last >= PUBLISHER_DECAY_MS)`. At exactly 60 s since last envelope, the publisher evicts this tick instead of one tick later. Plan's test now passes without timing-slop adjustments. | Otherwise the plan's test would need an extra 1100 ms advance to pass, which would mask the real boundary. Strict `>=` is more faithful to the spec's "last 60 s" phrasing and gives predictable test timing. |
| 2026-04-22 | v3 Task 6 review-fix (plan-vs-spec Critical): `useTopicStats` rolled the sparkline bucket ring on every 1 Hz tick, giving a 6-bucket × 1 s = 6 s window. Spec §6.1 line 168 literally specifies "6 buckets of 10 s each" = 60 s window. | Added `tickCountRef: number` that increments each tick; `rollBucket = tickCountRef.current % BUCKET_TICKS === 0` (BUCKET_TICKS = 10) gates the `shift()`/`push(0)` pair. Envelope absorption, publisher decay, EWMA, and recent5 rebuild continue to run every tick. Added regression test `sparkline rolls buckets every 10 seconds` pinning the behavior (tick 1 = rightmost 3, tick 10 = previous 3 shifted left one slot). | Spec wins over plan prose per `observatory/CLAUDE.md` authority ordering. The plan's code sample predated this review or conflated "1 Hz tick" with "1 s bucket" — the two are now distinct: tick cadence stays at 1 Hz for EWMA/decay/recent5 refresh, bucket roll runs at 0.1 Hz for the 60 s window. Flag for plan author. |
| 2026-04-22 | v3 Task 6 review-fix (Important): Topics row rendered 7 cells including an extra `kindTag` badge between topic and rate (plan Step 6 inherited from Firehose's kind column). Spec §6.1 line 168 lists exactly 5 content columns (topic · rate · sparkline · publishers · last-seen) plus a chevron per §6.1 line 172 — 6 grid cells total, not 7. | Removed the kind-badge `<span>` and dropped the `kindTag` import; grid-template-columns reduced to `1fr auto auto auto auto auto`. Also switched topic `<span>` from `font-mono text-[11px]` to plain `text-[11px]` per spec §4.3 "Row text: 11 px Inter 300" — tabular alignment comes from the grid, not a mono typeface. Row height pinned at `h-[22px]` (Task 5 precedent) per spec §6.1 line 169. | Kind information is redundant with the topic path column, which makes the prefix visually obvious (e.g. `hive/system/heartbeat` already reads as a heartbeat row). The Firehose tab keeps kindTag because its grid hides the topic suffix via truncation; Topics shows the full path, so the badge duplicates. Removing it also reclaims horizontal space for the topic column. |
| 2026-04-22 | v3 Task 6 review-fix (Important): spec line 443 named required tests "EWMA rate updates, sorting, row expand into recent-5" but the initial commit only covered empty-state + basic render. | Added two tests to `Topics.test.tsx`: (1) `sorts rows by ewmaRate descending` — two stats inserted with fast-last insertion order, verifies the sort survives insertion order; (2) `chevron click expands row to show recent 5` — uses `fireEvent.click` on the row's `<button>` chevron, verifies both source regions from `recent5` appear in the DOM after expand. Imported `fireEvent` and `TopicStat` at the top of the test file. Frontend suite 146 → 149. | Closes the spec-line-443 coverage gap. EWMA rate updates were already indirectly covered by the existing `accumulates after first tick` test (`ewmaRate > 0` assertion). |
| 2026-04-22 | v3 Task 7 Drift E interpretation: prompt wording "update metacog branch of third test" implies a single `mounts the placeholder for the active tab` test with per-tab branches, but Task 5 + Task 6 had already split that structure into distinct per-tab tests (`mounts the real Firehose on the firehose tab`, `mounts the real Topics component on the topics tab`). | Added a parallel `mounts the real Metacog component on the metacog tab` test asserting the `No metacognition events yet.` empty-state surfaces when `dockTab='metacog'`. Mirrors the Topics test structure. Resulting delta: +1 Dock test in addition to the +2 Metacog tests → 149 → 152 (prompt predicted 151). | Parallel per-tab tests stay consistent with Task 5/6 shape; collapsing back to a single multi-branch test would regress the pattern. The prompt's 151 target assumed reusing an existing metacog-branch test slot that no longer exists post-Task 6. Flag for plan/prompt author — the verification-gate line count should track the existing Dock.test.tsx shape. |
| 2026-04-22 | v3 Task 7 Metacog row-key shape drift: `FirehoseRow` uses `${observed_at}|${topic}|${source_region}` (three-part) as its React key + expand-state identity, but spec §7.2 line 208 specifies `${observed_at}|${topic}` (two-part) for `pendingEnvelopeKey` consumption in Messages (Task 10). | Metacog uses the spec's two-part key `${e.observed_at}|${e.topic}` for both the React `key` and the `envelopeKey` passed to `selectRegionFromRow`. Plan's code sample also uses this shape. | Matches the spec contract — the metacog tab is typically sparse (error/conflict/reflection events are rare), so collisions across identical ts+topic from different sources are unlikely and the Messages scroll contract is topic-scoped anyway. Firehose's three-part key is a local concern (its expand-state set needs row-identity, not topic-identity). |
| 2026-04-22 | v3 Task 8 Drift A (Appendix prop shape): plan Step 3 declared `{name, phase, lastErrorTs}` three-prop signature for `Appendix`, but plan Step 5's `Inspector.tsx` mount call `<Appendix name={name} phase={stats.phase} lastErrorTs={stats.last_error_ts} />` has no `stats` in scope — `Inspector.tsx` only has `displayName` (see `Inspector.tsx:38-80`). `Prompt.tsx`'s established pattern is `{name}`-only + internal `useStore((s) => s.regions[name]?.stats)` read. | Collapsed `Appendix`'s signature to `{name: string}` matching `Prompt`. Reads stats internally via `useStore((s) => s.regions[name]?.stats)`. `Inspector.tsx` mount becomes `<Appendix name={displayName} />` — same shape as `<Prompt name={displayName} />`. Tests seed store state via `useStore.setState({ regions: {...} })` before rendering. | Consistency with the existing section-mount contract avoids restructuring Inspector.tsx's displayName flow just for one new section. Flag for plan author — Task 8 Step 3 prop shape contradicts Step 5's mount call. |
| 2026-04-22 | v3 Task 8 Drift C (palette): plan Step 3 used `border-t border-[rgba(80,84,96,.35)]`, `summary.cursor-pointer px-3 py-1 text-[11px]`, which would have split the visual language with the existing Prompt (`border-b border-[#1f1f27]`, `summary.px-4 py-2`). | Matched `Prompt`'s palette: `border-b border-[#1f1f27]` frame, `px-4 py-2` summary, `#8a8e99` for secondary text, `#0e0e14` for `<pre>` background, `#1e1e26` for trigger tag chip, `#2a2a33` for button border, `#ff6a6a` for error. Max-height 360 px applied to the entries container (not the outer body) so the scroll band lives inside the details. | Consistency with the inspector slide-over's visual language per `observatory/CLAUDE.md` (v2 inspector palette is already frozen; rgba arbitrary values would have drifted). Flag for plan author. |
| 2026-04-22 | v3 Task 8 Drift D (empty-state vs loading): `useRegionAppendix` maps 404 → `data === ''` and non-404 → `error` set; `data === null` is "not loaded yet" (initial state or in-flight refetch). Plan Step 3 only branched on `data === ''`. | Guarded the empty-state render with `!loading && !error && data === '' && (...)` so the "No appendix yet — region hasn't slept." copy only appears after the fetch resolves to the 404 path. The loading branch (`loading && ...`) covers both first-load-`data===null` and reload-in-flight. Entries branch (`!loading && !error && entries.length > 0 && ...`) renders only when parsed entries exist. | Prevents the empty-state flashing during initial fetch (before 404 → `''` resolves) and prevents double-render during reload. Matches the hook's documented state machine. |
| 2026-04-22 | v3 Task 9 Drift A (region_health per_region schema): plan Step 4's test fixture `per_region: { pfc: 'alive', amygdala: 'stale', acc: 'dead' }` has each entry as a bare status string, but `glia/metrics.py::build_region_health_payload` lines 221–235 emit `per_region[name] = {status, consecutive_misses, uptime_s}` — object values, not strings. `status` also carries the raw `LifecyclePhase` value (`"wake"`, `"sleep"`, `"bootstrap"`, `"shutdown"`) plus `"dead"` on LWT, NOT the `alive | stale | dead | unknown` vocabulary spec §10.1 defines. | Implemented a `livenessOf(entry)` helper in `hud/SystemMetrics.tsx` that derives the four-value liveness vocabulary from the real payload: `status === 'dead'` → `dead`, `consecutive_misses > 0` → `stale`, else `alive` (`unknown` reserved for entries absent from the map). Test fixture rewritten to use the real object shape (`{status: 'wake', consecutive_misses: 0, uptime_s: 100}` etc.) so all three liveness arms are exercised. `HEALTH_COLOR` is a `Record<Liveness, string>` for exhaustive type-safety. | Reality wins over plan prose per `observatory/CLAUDE.md` authority ordering. Without this, the SystemMetrics tile would render only `#888888`-ish (unknown) cells against real glia-emitted payloads, because `HEALTH_COLOR[status]` would index by e.g. `"wake"` which the plan's color map doesn't recognize. Flag for plan author that Task 9 Step 4 fixture contradicts the actual payload shape. |
| 2026-04-22 | v3 Task 9 Drift B (Hud pointer-events + layout): plan Step 8 replaced `<div absolute top-3 left-3 pointer-events-none>` with `<div fixed top-3 left-3 ...>` dropping pointer-events entirely. Would break two things: (1) SelfState's new tab buttons need pointer events to register clicks, and (2) the gap between tiles should stay click-through to the scene behind (scene OrbitControls are region-interactable there). | Preserved `absolute top-3 left-3` + `pointer-events-none` on the container and added `[&>*]:pointer-events-auto` (Tailwind 3.3+ arbitrary variant; project is on 3.4.19) so each direct child tile receives pointer events while gaps pass through. Verified `npx vitest run` still green (164/164). | `[&>*]` scoping is narrower than a blanket `pointer-events-auto` on the container — the latter would block scene clicks anywhere inside the 288-px max-width column even outside the tile backgrounds. `absolute` (not `fixed`) matches existing HUD positioning inside the `.relative` App wrapper; spec §10.1 line 314 only prescribes tile ordering, not positioning. |
| 2026-04-22 | v3 Task 9 Drift D (applyRetained atomicity + unknown-modulator fall-through): existing `applyRetained` had `if (!isModulatorName(name)) return;` which early-exited for unknown modulator names, meaning those topics would NOT land in the new `retained` raw map. Plan Step 2 sketched a separate `set({ retained: ... })` call after the ambient body, but two separate `set(...)` calls = two subscriber notifications = potential half-updated render. | Restructured the branch: unknown modulator names still fall through to a single atomic `set({ ambient, retained: { ...get().retained, [topic]: payload } })` at the end. `ambient.modulators[name] = ...` only runs when `isModulatorName(name)` is true, so the existing "drops unknown modulator names from ambient" test still passes. Every retained topic — modulator (known or unknown), self, metrics, anything else — ends up in the raw `retained` map. | Single `set(...)` gives transactional snapshot consistency for zustand subscribers. Unknown modulator names ending up in raw `retained` is a feature: the dock Firehose/Topics tabs can still surface the topic for debugging even if Modulators tile rejects the name. Existing `drops unknown modulator names from live applyRetained` test validates ambient rejection; new `retained map captures snapshot + live updates` test validates the raw map is populated. |
| 2026-04-22 | v3 Task 9 review-fix spec gap (SystemMetrics tooltip): spec §10.1 line 312 prescribes tooltip format `{regionName} · {status} · last-hb HH:MM:SS`, but `glia/metrics.py::build_region_health_payload` per_region schema (glia/metrics.py:221-235) emits `{status, consecutive_misses, uptime_s}` — there is NO `last_heartbeat_ts` field on the wire. Cross-project gap: the observatory cannot render `last-hb HH:MM:SS` without a glia extension or a spec amendment. | Tooltip simplified to `{name} · {status} · {live}` — the raw `LifecyclePhase` status (`"wake"`/`"sleep"`/…/`"dead"`) plus the derived `alive | stale | dead | unknown` bucket so operators can still distinguish "is the region nominally up (status)" from "is the heartbeat stream healthy (live)". The `last-hb` field is deferred pending spec/glia resolution. | Flag for Larry: either (a) amend spec §10.1:312 to drop `last-hb HH:MM:SS` from the tooltip format, or (b) extend `build_region_health_payload` per_region entries with `last_heartbeat_ts` so the observatory can render the prescribed format. Observatory is read-only toward Hive so it cannot add the field itself. |
| 2026-04-22 | v3 Task 9 review-fix spec gap (Identity tab render): spec §10.2 line 324 describes Identity as a "key-value list" of top-level string/number fields, but Task 2's `extractAmbient` coerces `hive/self/identity` payloads via `identity: String(value ?? '')` (store.ts), so by the time `SelfState.tsx` reads `self.identity`, only a bare string survives. For the common payload shape (`{"value": "<text>"}` → `identity = "<text>"`), the current paragraph render is effectively a single-row key-value. Richer dict-shaped identity payloads would already hit the object branch at `SelfState.tsx` line 56 correctly, so only the `{value: "..."}` → string projection case loses the key-value framing. | Accept current behavior as a known spec-vs-projection gap. Re-widening `ambient.self.identity` to preserve the full dict would require reverting Task 2's projection contract (`store.ts` + `extractAmbient`) and retesting every downstream consumer. Not worth the churn for the common case, and the object branch already handles richer payloads. | Flag for Larry: if spec §10.2:324 really intends dict-shaped identity payloads with multiple top-level fields (e.g. `{name: "Hive", purpose: "observe", age_days: 42}`) to render as a key-value list, we can un-coerce `identity` in `extractAmbient` back to raw `unknown`. Current behavior is correct for today's mPFC output (`{"value": "..."}`). |
| 2026-04-22 | v3 Task 10 prompt claimed `jsdom 20+ implements scrollIntoView as a no-op (doesn't throw)`, but this workspace pins `jsdom ^29.0.2` and the method is still undefined on Element — the pendingEnvelopeKey test crashed with `TypeError: node.scrollIntoView is not a function` before any of the expand/clear side effects ran. Mocking `Element.prototype.scrollIntoView` in the test would couple the test to the implementation detail; leaving the component to crash in non-browser mounts (SSR, jest/node runtime, future headless worker) is a latent bug. | Wrapped the call in `if (typeof node.scrollIntoView === 'function')` inside `Messages.tsx`'s pendingKey effect. Production renders unchanged (browser DOM always has the method); jsdom tests + any future non-browser environment no-op the scroll cleanly, then fall through to the expand + `followTailRef=false` + `setPendingKey(null)` logic per spec §7.2 "consumes + clears". | Keeps the production `scrollIntoView({ block: 'nearest', behavior: 'smooth' })` contract while surviving the test environment and any future non-browser mount. The added `typeof` check costs one operation per pendingKey consumption — negligible versus a mocked `HTMLElement.prototype.scrollIntoView = () => {}` shim in `vitest.setup.ts` that would hide similar breakage elsewhere. Flag for prompt author: the gotcha about jsdom no-op applies to jsdom's default DOM impl, but scrollIntoView specifically is still unimplemented in jsdom 16+ (issue #1695) — keep the guard in place. |
| 2026-04-22 | v3 Task 10 MAX_ROWS test selector broke under Drift C: the existing `caps rendered rows at MAX_ROWS` test queried `.grid.grid-cols-\\[60px_14px_1fr\\]`, but Drift C adds a chevron column → `grid-cols-[60px_14px_1fr_16px]` → exact-className selector misses every row. | Switched both the MAX_ROWS test and the new chevron-click test to query `[data-testid="message-row-grid"]`, and tagged the row's inner `.grid` div with that testid in `Messages.tsx`. Selector is stable across future column-count changes; other Messages styling tests don't rely on this class. | Keeping the className-escape selector would require updating the string every time the grid layout evolves. A testid is cheap, semantic ("this element is the message row grid"), and unambiguous. Does not shadow any other usage in the tree. |
