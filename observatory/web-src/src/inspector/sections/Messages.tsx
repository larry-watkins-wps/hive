// TODO(Task 14): Implement Messages section — expanded-by-default scrollable
// list of the last ~100 envelopes where source_region == name or name is in
// destinations, filtered from the store's `recent` ring buffer. Row layout
// `HH:MM:SS · ↑/↓ · topic · payload-preview (first 80 chars)`, click-to-
// expand full envelope JSON, auto-scroll when within 40 px of bottom.
// Spec §3.2 item 5.
//
// Task 12 ships this as a stub so Inspector.tsx compiles without pulling
// Task-14 concerns forward.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function Messages(_props: { name: string }) {
  return null;
}
